const getPrompt = (bedrooms) =>
  `Architectural 3D top-down view of a ${bedrooms} apartment. Show exact walls, rooms, doors and windows. Simple modern interior, white walls, light floor, basic furniture. Clean white background. Architectural visualization style.`;

module.exports = getPrompt;